<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CAS | Personal Details</title>
  <link href="../header.css" rel="stylesheet" type="text/css" />
  <link href="../sidenav.css" rel="stylesheet" type="text/css" />
  <link href="../tables.css" rel="stylesheet" type="text/css" />
</head>

<header>
  <div class='main'>
    <%- include('../../partials/supplier/header') %>
  </div>
</header>

<body>
  <% if(details.status) {
    if(details.status === 'registered' || details.status === 'pending') { %>
  <%- include('../../partials/supplier/pendingSidenav') -%>
  <% } else if(details.status === 'approved') { %>
  <%- include('../../partials/supplier/sidenav') -%>
  <% }
  } %>

  <div class='main'>
    <h1>Clinical Automation System</h1>
    <br>
    <h2>My Personal Details</h2>
    <br>
    <tr>
      <td>
        Name
      </td>
      <td>
        <%= details.name %>
      </td>
    </tr>
    <tr>
      <td>
        Email
      </td>
      <td>
        <%= details.email %>
      </td>
    </tr>
    <tr>
      <td>
        <% if (details.companyName && details.companyAddress) { %>
        Company Name
      </td>
      <td>
        <%= details.companyName %>
      </td>
    </tr>
    <tr>
      <td>
        Company Address
      </td>
      <td>
        <%= details.companyAddress %>
      </td>
    </tr>
    <% } else { %>
    <form action="/supplier/details" method="POST">
      <tr>
        <td>
          Company Name
        </td>
        <td>
          <input type="text" name="companyName" required="required">
        </td>
      </tr>
      <tr>
        <td>
          Company Address
        </td>
        <td>
          <input type="text" name="companyAddress" required="required">
        </td>
      </tr>
      <br>
      <button type="submit">Submit</button>
    </form>
    <% } %>
  </div>
</body>

</html>
