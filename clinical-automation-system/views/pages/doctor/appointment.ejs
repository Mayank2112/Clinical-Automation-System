<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CAS | Appointment List</title>
  <link href="../header.css" rel="stylesheet" type="text/css" />
  <link href="../sidenav.css" rel="stylesheet" type="text/css" />
</head>

<header>
  <div class='main'>
    <%- include('../../partials/doctor/header') %>
  </div>
</header>
<body>
  <%- include('../../partials/doctor/approvedSidenav') -%>

  <div class='main'>
    <h1>Clinical Automation System</h1>
    <br>
    <h2>Today's Appointments</h2>
    <br>
    <%= message %>
    <% if(!message && details.length) { %>
      <table border="1px">
        <th>Patient</th>
        <th>Date</th>
        <th>Time</th>
        <th>Subject</th>
        <th>Description</th>
        <th>Done</th>
        <th>Cancel</th>
        <th>Response</th>
        <% details.forEach(appointment => { %>
        <form action="/doctor/appointment" method="post">
          <tr>
            <td><input type="text" name="patientId" readonly="readonly" value= <%= appointment.patientId %> ></td>
            <td><%= appointment.date %></td>
            <td><%= appointment.time %></td>
            <td><%= appointment.subject %></td>
            <td><%= appointment.description %></td>
            <td><input type="radio" name="status" value="done"></td>
            <td><input type="radio" name="status" value="canceled"></td>
            <td><button type="submit">Send</button></td>
          </tr>
        </form>
        <% }) %>
        <% } %>
  </div>
</body>

</html>
